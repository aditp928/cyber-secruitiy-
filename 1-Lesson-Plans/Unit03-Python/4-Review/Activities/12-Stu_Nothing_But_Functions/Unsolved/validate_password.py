# TODO: Define a function, called `validate_password`, which accepts a `password`
# TODO: Inside the function, check if the password is longer than 6 characters long
# TODO: If it is, return `True`
# TODO: If it isn't, return `False`
